# Express Server Documentation (Placeholder)

This folder is reserved for Express-specific server guidance.

Use this layer for routing, middleware, request/response integration, and Express-only operational patterns.

Keep core architecture rules in `server/core/`.

